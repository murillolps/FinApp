services:
  mysql:
    image: mysql:8.0
    container_name: finapp-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: finapp
      MYSQL_DATABASE: finapp
    ports:
      - "3306:3306"
    volumes:
      - finapp_mysql_data:/var/lib/mysql

volumes:
  finapp_mysql_data:
